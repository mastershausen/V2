/**
 * @file types/auth/statusTypes.ts
 * @description Re-Exporte der Auth-Status-Typen
 * 
 * Diese Datei ist ein Kompatibilitätsmodul, das die Auth-Status-Typen
 * aus dem features-Verzeichnis re-exportiert, für Code, der noch die alten
 * Import-Pfade verwendet.
 */

export { AuthStatus, AuthenticatedStatus, UnauthenticatedStatus, LoadingStatus } from '@/features/auth/types'; 