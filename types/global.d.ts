// globale TypeScript-Deklarationen für die App

/**
 * Erweitert die globalen Typen für das Ausführen der App
 */
export {};

declare global {
  /**
   * Flag für den Entwicklungsmodus
   */
  var __DEV__: boolean;
} 