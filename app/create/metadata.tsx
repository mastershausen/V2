import React from 'react';
import MetaDataScreen from '@/features/shared/screens/MetaDataScreen';

/**
 * Gemeinsame Route für den Metadaten-Screen
 * Wird sowohl für Gigs als auch für Fallstudien verwendet
 */
export default function MetaDataRoute() {
  return <MetaDataScreen />;
} 