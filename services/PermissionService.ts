/**
 * PermissionService - Reexport
 * 
 * Reexportiert den PermissionService aus dem Unterverzeichnis, 
 * um Kompatibilität mit bestehenden Imports zu gewährleisten
 */

import { PermissionService } from './permissions';

export default PermissionService;
