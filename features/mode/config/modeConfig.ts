/**
 * @file features/mode/config/modeConfig.ts
 * @description Zentrale Konfiguration für das Mode-Feature
 * 
 * Diese Datei enthält alle Konstanten, Defaults und Konfigurationsparameter
 * für die Mode-Funktionalität. Sie dient als zentrale Stelle für alle Einstellungen.
 */

// Vorläufiger Platzhalter - wird in Schritt 3 mit konkreter Konfiguration gefüllt
// Die tatsächlichen Konfigurationen werden später aus den einzelnen Modulen hierher migriert

// Beispiel für Mode-Farben
export const MODE_COLORS = {
  DEMO_MODE: '#FFB020',
  LIVE_MODE: '#14B8A6'
}; 